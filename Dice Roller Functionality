from random import randint as Roll

a=int(input("How many sides?"))
b=int(input("How many die?"))

class Dice():
    def roll(sides, numDice=1):
        RunTot=0
        for i in range(numDice):
            RunTot=Roll(1,sides)+RunTot
        return RunTot
print(Dice.roll(a,b))